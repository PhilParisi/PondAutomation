rpi_connections = {

    # these parameters specify the pins used on the RPi board for connections 
    # your physical wiring should match these parameters below
    # we use pin #'s, not BCM in the code, but BCM values are provided here as well

    # red LED pin [BCM 16]
    'red_LED':36,

    # yellow LED pin [BCM 20]
    'yellow_LED':38,

    # green LED pin [BCM 21]
    'green_LED':40

}